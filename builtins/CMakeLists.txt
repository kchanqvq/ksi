include_directories(.)

file(GLOB ALL_FILE ./*)
set(ALL_DIRS "")
foreach(f ${ALL_FILE})
if(IS_DIRECTORY ${f})
LIST(APPEND ALL_DIRS ${f})
endif()
endforeach()
include_directories(src)
foreach(p ${ALL_DIRS})
add_subdirectory(${p})
endforeach()
set(builtins_lib ${builtins_lib} PARENT_SCOPE)
